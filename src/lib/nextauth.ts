// NextAuth integration was intentionally removed. This file remains as a
// noop placeholder to avoid build-time import errors while the project uses
// its custom authentication routes.
export const nextAuthDisabled = true

export default nextAuthDisabled
